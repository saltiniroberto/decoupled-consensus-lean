name = "decoupled-consensus-lean"
version = "0.1.0"
defaultTargets = ["Spec", "Analysis"]

# The STS framework, as a git submodule at `deps/lean-sts` and a path require. A submodule
# rather than a git require so that the revision this project builds against is recorded in
# this repository's own history: `git submodule status` says which one, and a `git bisect`
# here moves it.
#
# The flavour used is `StsMultisetLog` (`import StsMultisetLog.Spec.Protocol`); the package
# declares four others, and nothing is shared between them, so importing one does not build
# the rest.
[[require]]
name = "sts-framework"
path = "deps/lean-sts"

# Mathlib's rev must match `lean-toolchain` (leanprover/lean4:v4.32.2), and must match the
# rev `deps/lean-sts` requires: Lake resolves one rev per dependency name across the whole
# build graph. All three are v4.32.2 today. Bump them together.
[[require]]
name = "mathlib"
scope = "leanprover-community"
rev = "v4.32.2"

# This project's own sources. `srcDir` is `lean` and the module root is `Spec`, so a module
# `Spec.Defs.Basic` is the file `lean/Spec/Defs/Basic.lean`. Lake derives a module's path from
# its name and nothing else — measured, not assumed — so every directory under `srcDir`
# repeats a module component. Naming the root `Spec` rather than `Decoupled.Spec` is what
# keeps that short.
#
# `Spec/` itself holds only the figure translations, `Fig<n><Subject>.lean`. What they are
# written in terms of is one level down in `Spec/Defs/`, so listing `Spec/*.lean` is the list
# of figures rendered so far.
#
# The analysis is a second `lean_lib` below, over the same `srcDir` with root `Analysis`, so
# `lean/Analysis/…`. The two globs stay disjoint because the roots differ.
#
# `tools/decl_index.lean` filters on module roots, and its `modRoots` list is what has to name
# both — there is no single shared prefix over the two halves.
[[lean_lib]]
name = "Spec"
srcDir = "lean"
globs = ["Spec", "Spec.+"]

# The analysis: the paper's numbered results, stated and proved against the specification
# above. Root `Analysis`, same `srcDir`, so `Analysis.Lemmas` is `lean/Analysis/Lemmas.lean`.
[[lean_lib]]
name = "Analysis"
srcDir = "lean"
globs = ["Analysis", "Analysis.+"]
